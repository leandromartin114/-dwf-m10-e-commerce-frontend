// import Carousel from "better-react-carousel";
import { Card } from "ui/card";

export function MyCarousel({ array }: any) {
	return <div></div>;
	// return (
	// 	<Carousel loop style={{ display: "flex", justifyContent: "center" }}>
	// 		{array.map((p: any) => (
	// 			<Carousel.Item
	// 				style={{ display: "flex", justifyContent: "center" }}
	// 				key={p.objectID}
	// 			>
	// 				<Card
	// 					imgURL={p.Images[0].url}
	// 					name={p.Name}
	// 					price={p["Unit cost"]}
	// 					color='var(--fucsia)'
	// 				></Card>
	// 			</Carousel.Item>
	// 		))}
	// 	</Carousel>
	// );
}
